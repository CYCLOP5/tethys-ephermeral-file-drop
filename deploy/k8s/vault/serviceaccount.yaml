apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault
  namespace: tethys
  # When switching to IRSA/pod-identity, annotate with the IAM role ARN here.
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<account>:role/tethys-dev-vault
automountServiceAccountToken: false
